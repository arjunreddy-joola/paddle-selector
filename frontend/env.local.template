# Local development only — proxies /api/* to the FastAPI backend.
# Copy to .env.local to activate the proxy:
#   cp env.local.template .env.local
# Not needed (and must NOT be set) on Vercel — Vercel routes /api/* directly.
BACKEND_URL=http://localhost:4000
